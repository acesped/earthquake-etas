name: ETAS Scheduler Test

on:
  schedule:
    - cron: "11,21,31,41,51 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test scheduler
        run: |
          echo "Event: ${{ github.event_name }}"
          date -u
